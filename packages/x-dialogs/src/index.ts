export { DialogsProvider } from './DialogsProvider';
export { closeAllDialogs, closeDialog, dialogs, openConfirmDialog, openContextDialog, openDialog } from './events';
export { useDialogs } from './use-dialogs/use-dialogs';

export type { DialogsProviderProps } from './DialogsProvider';

export type { ContextDialogProps, Dialog, Dialogs, DialogsOverride } from './context';
